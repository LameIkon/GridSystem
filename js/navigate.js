function navigateToLevel(mapID) {
    const path = 'level-' + mapID
    window.location.href = '../levels/' + path + '.html'
}

function navigateToPage(page) {
    window.location.href = '../menus/' + page + '.html'
}